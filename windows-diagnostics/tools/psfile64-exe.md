**psfile64.exe** [ADMIN]
Purpose: List files opened remotely on the local system via network shares.
Flags: `-accepteula`
Output: Plain text.
Example: `.\systeminternals\psfile64.exe -accepteula | findstr /V "^$"`

