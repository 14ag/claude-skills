**pagedfrg.exe** [ADMIN][DRIVER]
Purpose: Defragment paged pool, non-paged pool, and registry hives.
Flags: GUI-only. Loads kernel driver; unload on exit.
Output: GUI-only.
Example: `.\systeminternals\pagedfrg.exe` (GUI-only — requires reboot to complete)


### D. Registry

