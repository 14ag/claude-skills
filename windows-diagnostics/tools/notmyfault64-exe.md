**notmyfault64.exe** [ADMIN][DRIVER][DESTRUCTIVE]
Purpose: Deliberately crash the system or leak memory for testing crash dump configuration.
Flags: `-accepteula`. GUI-only.
Prerequisites: Explicit user confirmation required. Will crash or destabilise the system.
Output: GUI-only.
Example: `.\systeminternals\notmyfault64.exe /accepteula` (confirm with user first — causes crash)

