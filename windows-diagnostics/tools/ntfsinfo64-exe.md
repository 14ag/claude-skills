**ntfsinfo64.exe** [ADMIN]
Purpose: Display NTFS volume metadata (cluster size, MFT location, etc.).
Flags: `-accepteula <drive letter or volume>`
Output: Plain text key-value pairs.
Example: `.\systeminternals\ntfsinfo64.exe -accepteula C:`

