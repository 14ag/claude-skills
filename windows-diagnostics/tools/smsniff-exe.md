**smsniff.exe** (NirSoft) [CAPTURE]
Purpose: Packet sniffer that captures raw network packets.
Flags: GUI-only. No reliable CLI export.
Output: GUI-only. Instruct user to use File > Save to capture file manually.
Prerequisites: Admin required. Warn user that captured traffic may contain credentials.
Example: `.\nirsoft\smsniff.exe` (GUI-only — instruct user to save capture manually)

